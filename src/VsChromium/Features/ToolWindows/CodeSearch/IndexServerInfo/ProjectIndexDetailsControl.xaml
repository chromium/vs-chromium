<UserControl x:Class="VsChromium.Features.ToolWindows.CodeSearch.IndexServerInfo.ProjectIndexDetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VsChromium.Features.ToolWindows.CodeSearch.IndexServerInfo"
             xmlns:typedMessages="clr-namespace:VsChromium.Core.Ipc.TypedMessages;assembly=VsChromium.Core"
             xmlns:wpf="clr-namespace:VsChromium.Wpf"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=typedMessages:ProjectDetails, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="600">
  <UserControl.Resources>
    <wpf:HumanReadableBytesValueConverter x:Key="HumanReadableBytesValueConverter" />
    <wpf:NumberValueConverter x:Key="NumberValueConverter" />
  </UserControl.Resources>
  <DockPanel>
    <TextBlock Margin="0,4,0,4" DockPanel.Dock="Top">Files sorted by extension and total size</TextBlock>
    <ListView x:Name="ListView" ItemsSource="{Binding FilesByExtensionDetails}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Extension" Width="120" DisplayMemberBinding="{Binding FileExtension}" />
          <GridViewColumn Header="File count" Width="120">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding FileCount, Converter={StaticResource NumberValueConverter}}" TextAlignment="Right"/>
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
          <GridViewColumn Header="File size" Width="120">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding FilesByteLength, Converter={StaticResource HumanReadableBytesValueConverter}}" TextAlignment="Right"/>
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
        </GridView>
      </ListView.View>
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
      </ListView.ItemContainerStyle>
    </ListView>
  </DockPanel>
</UserControl>
